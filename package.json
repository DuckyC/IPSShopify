{
  "name": "ipsshopify",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@shopify/graphql-client": "^1.2.1",
    "tedious": "^18.6.1",
    "winston": "^3.17.0",
    "winston-daily-rotate-file": "^5.0.0"
  },
  "scripts": {
    "run": "bun run index.ts",
    "build-windows": "bun build --compile --minify --sourcemap --target=bun-windows-x64 index.ts --outfile out/IPSShopify.exe",
    "build": "bun build --compile --minify --sourcemap index.ts --outfile out/IPSShopify"
  }
}